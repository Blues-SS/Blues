CREATE TABLE SPRINT (
  ID_SPRINT SERIAL PRIMARY KEY,
  NOME VARCHAR(200),
  STATUS VARCHAR(200),
  DT_CRIACAO TIMESTAMP,
  DT_ALTERACAO TIMESTAMP
);

CREATE TABLE STATUS (
  ID_STATUS SERIAL PRIMARY KEY,
  DS_STATUS VARCHAR(200)
);

CREATE TABLE HISTORIA (
  ID_HISTORIA SERIAL PRIMARY KEY,
  ID_SPRINT INTEGER ,
  ID_STATUS INTEGER,
  NOME_HISTORIA VARCHAR(20),
  VALUE_BUSINESS INTEGER,
  DT_CRIACAO TIMESTAMP,
  DT_ALTERACAO TIMESTAMP
);
ALTER TABLE HISTORIA ADD CONSTRAINT FK_SPRINT FOREIGN KEY (ID_SPRINT) REFERENCES SPRINT (ID_SPRINT) MATCH FULL;
ALTER TABLE STATUS ADD CONSTRAINT FK_STATUS FOREIGN KEY (ID_STATUS) REFERENCES SPRINT (ID_STATUS) MATCH FULL;

drop table SPRINT


